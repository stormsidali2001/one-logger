/**
 * This file is auto-generated by ioc-arise.
 * Do not modify this file manually.
 */
import { Container, Lifecycle } from '@notjustcoders/di-container';
import type { ContainerRegistry } from './container.gen.d';
import { UpdateTrace } from './src/use-cases/updateTrace';
import { UpdateSpan } from './src/use-cases/updateSpan';
import { UpdateProjectConfig } from './src/use-cases/updateProjectConfig';
import { UpdateProject } from './src/use-cases/updateProject';
import { StopServer } from './src/use-cases/stopServer';
import { StopMCPServer } from './src/use-cases/stopMCPServer';
import { SetConfig } from './src/use-cases/setConfig';
import { RestartServer } from './src/use-cases/restartServer';
import { RestartMCPServer } from './src/use-cases/restartMCPServer';
import { GetTracesByProjectId } from './src/use-cases/getTracesByProjectId';
import { GetTraceWithSpans } from './src/use-cases/getTraceWithSpans';
import { GetTraceById } from './src/use-cases/getTraceById';
import { GetSpansByTraceId } from './src/use-cases/getSpansByTraceId';
import { GetServerLogs } from './src/use-cases/getServerLogs';
import { GetProjectMetrics } from './src/use-cases/getProjectMetrics';
import { GetProjectConfig } from './src/use-cases/getProjectConfig';
import { GetProjectById } from './src/use-cases/getProjectById';
import { GetMetadataKeysByProjectId } from './src/use-cases/getMetadataKeysByProjectId';
import { GetMCPServerLogs } from './src/use-cases/getMCPServerLogs';
import { GetLogsByProjectId } from './src/use-cases/getLogsByProjectId';
import { GetLogById } from './src/use-cases/getLogById';
import { GetHistoricalLogCounts } from './src/use-cases/getHistoricalLogCounts';
import { GetFilteredLogs } from './src/use-cases/getFilteredLogs';
import { GetConfig } from './src/use-cases/getConfig';
import { GetAllProjects } from './src/use-cases/getAllProjects';
import { GetAllLogs } from './src/use-cases/getAllLogs';
import { GetAllConfig } from './src/use-cases/getAllConfig';
import { DeleteProject } from './src/use-cases/deleteProject';
import { CreateTrace } from './src/use-cases/createTrace';
import { CreateSpan } from './src/use-cases/createSpan';
import { CreateProject } from './src/use-cases/createProject';
import { CreateLog } from './src/use-cases/createLog';
import { CreateBulkLog } from './src/use-cases/createBulkLog';
import { ClearServerLogs } from './src/use-cases/clearServerLogs';
import { ClearProjectTraces } from './src/use-cases/clearProjectTraces';
import { ClearProjectLogs } from './src/use-cases/clearProjectLogs';
import { ClearMCPServerLogs } from './src/use-cases/clearMCPServerLogs';
import { BulkCreateTraces } from './src/use-cases/bulkCreateTraces';
import { WebServerManager } from './src/server/webServerManager';
import { ServerManager } from './src/server/serverManager';
import { ServerLogger } from './src/server/serverLogger';
import { MCPServerManager } from './src/server/mcpServerManager';
import { TraceRepository } from './src/repositories/traceRepository';
import { ProjectRepository } from './src/repositories/projectRepository';
import { LogRepository } from './src/repositories/logRepository';
import { ConfigRepository } from './src/repositories/configRepository';
import { DatabaseClient } from './src/db/db';

export const container = new Container<ContainerRegistry>();

container.register(UpdateTrace, {
  useClass: UpdateTrace,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(UpdateSpan, {
  useClass: UpdateSpan,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(UpdateProjectConfig, {
  useClass: UpdateProjectConfig,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(UpdateProject, {
  useClass: UpdateProject,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(StopServer, {
  useClass: StopServer,
  dependencies: [ServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(StopMCPServer, {
  useClass: StopMCPServer,
  dependencies: [MCPServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(SetConfig, {
  useClass: SetConfig,
  dependencies: [ConfigRepository, ServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(RestartServer, {
  useClass: RestartServer,
  dependencies: [ServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(RestartMCPServer, {
  useClass: RestartMCPServer,
  dependencies: [MCPServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetTracesByProjectId, {
  useClass: GetTracesByProjectId,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetTraceWithSpans, {
  useClass: GetTraceWithSpans,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetTraceById, {
  useClass: GetTraceById,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetSpansByTraceId, {
  useClass: GetSpansByTraceId,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetServerLogs, {
  useClass: GetServerLogs,
  dependencies: [ServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetProjectMetrics, {
  useClass: GetProjectMetrics,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetProjectConfig, {
  useClass: GetProjectConfig,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetProjectById, {
  useClass: GetProjectById,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetMetadataKeysByProjectId, {
  useClass: GetMetadataKeysByProjectId,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetMCPServerLogs, {
  useClass: GetMCPServerLogs,
  dependencies: [MCPServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetLogsByProjectId, {
  useClass: GetLogsByProjectId,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetLogById, {
  useClass: GetLogById,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetHistoricalLogCounts, {
  useClass: GetHistoricalLogCounts,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetFilteredLogs, {
  useClass: GetFilteredLogs,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetConfig, {
  useClass: GetConfig,
  dependencies: [ConfigRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetAllProjects, {
  useClass: GetAllProjects,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetAllLogs, {
  useClass: GetAllLogs,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(GetAllConfig, {
  useClass: GetAllConfig,
  dependencies: [ConfigRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(DeleteProject, {
  useClass: DeleteProject,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(CreateTrace, {
  useClass: CreateTrace,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(CreateSpan, {
  useClass: CreateSpan,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(CreateProject, {
  useClass: CreateProject,
  dependencies: [ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(CreateLog, {
  useClass: CreateLog,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(CreateBulkLog, {
  useClass: CreateBulkLog,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(ClearServerLogs, {
  useClass: ClearServerLogs,
  dependencies: [ServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(ClearProjectTraces, {
  useClass: ClearProjectTraces,
  dependencies: [TraceRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(ClearProjectLogs, {
  useClass: ClearProjectLogs,
  dependencies: [LogRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(ClearMCPServerLogs, {
  useClass: ClearMCPServerLogs,
  dependencies: [MCPServerManager],
  lifecycle: Lifecycle.Singleton,
});

container.register(BulkCreateTraces, {
  useClass: BulkCreateTraces,
  dependencies: [TraceRepository, ProjectRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(WebServerManager, {
  useClass: WebServerManager,
  lifecycle: Lifecycle.Singleton,
});

container.register(ServerManager, {
  useClass: ServerManager,
  dependencies: [ConfigRepository],
  lifecycle: Lifecycle.Singleton,
});

container.register(ServerLogger, {
  useClass: ServerLogger,
  lifecycle: Lifecycle.Singleton,
});

container.register(MCPServerManager, {
  useClass: MCPServerManager,
  dependencies: [ConfigRepository, ServerLogger],
  lifecycle: Lifecycle.Singleton,
});

container.register(TraceRepository, {
  useClass: TraceRepository,
  lifecycle: Lifecycle.Singleton,
});

container.register(ProjectRepository, {
  useClass: ProjectRepository,
  lifecycle: Lifecycle.Singleton,
});

container.register(LogRepository, {
  useClass: LogRepository,
  lifecycle: Lifecycle.Singleton,
});

container.register(ConfigRepository, {
  useClass: ConfigRepository,
  lifecycle: Lifecycle.Singleton,
});

container.register(DatabaseClient, {
  useClass: DatabaseClient,
  lifecycle: Lifecycle.Singleton,
});
